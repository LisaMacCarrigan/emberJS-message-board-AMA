{{#if editQuestionForm}}
  <h5>Edit {{question.author}}'s Q: {{question.question}}</h5>
  <div>
    <form>
      <div class="form-group">
        <label for="input_question">Question</label>
        {{input class="form-control" value=input_question id="input_question" placeholder=question.question}}
      </div>

      <div class="form-group">
        <label for="type">Author</label>
        {{input class="form-control" value=author id="author" placeholder=question.author}}
      </div>

      <div class="form-group">
        <label for="city">Notes</label>
        {{input class="form-control" value=notes id="notes" placeholder=question.notes}}
      </div>

      <button {{action 'edit' question}} class="btn btn-success btn-md">
        <span class="glyphicon glyphicon-plus"></span> Resubmit
      </button>

    </form>
  </div>
{{else}}
  <button {{action 'editQuestionForm'}} class="btn btn-default btn-sm">
    <span class="glyphicon glyphicon-pencil"></span> Edit
  </button>
{{/if}}
