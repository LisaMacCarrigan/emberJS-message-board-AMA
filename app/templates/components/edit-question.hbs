{{#if editQuestionForm}}
  <h5>Edit {{question.author}}'s Q: {{question.question}}</h5>
  <div>
    <form>
      <div class="form-group">
        <label for="input_question">Question</label>
        {{input value=input_question id="input_question" placeholder=question.question}}
      </div>

      <div class="form-group">
        <label for="type">Author</label>
        {{input value=author id="author" placeholder=question.author}}
      </div>

      <div class="form-group">
        <label for="city">Notes</label>
        {{input value=notes id="notes" placeholder=question.notes}}
      </div>

      <button {{action 'edit' question}} class="btn btn-default btn-xs">
        <span class="glyphicon glyphicon-plus"></span> Resubmit
      </button>

    </form>
  </div>
{{else}}
  <button {{action 'editQuestionForm'}} class="btn btn-default btn-xs">
    <span class="glyphicon glyphicon-pencil"></span> Edit
  </button>
{{/if}}
